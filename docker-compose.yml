version: '3.8'

services:
  app:
    build: .
    ports:
      - "10000:10000"
    volumes:
      - ./data:/app/data
    environment:
      - SPRING_H2_CONSOLE_ENABLED=true
      - SPRING_H2_CONSOLE_SETTINGS_WEB-ALLOW-OTHERS=true
